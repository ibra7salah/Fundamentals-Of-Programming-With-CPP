//لدينا اكثر من كود فيهم مشاكل تحتاج للتصليح أو إضافة الناقص وكل ما عليك كتابة الكود السليم بعد التصليح
// الهدف من التصليح معرفتك ب (syntax)   .وطريقة كتابة الكود والتعود على الفهم وليس الحفظ
//الكود التالي يحتوي على 7 أخطاء يجب عليك إصلاحهم لتخرج بالنتيجة في المثال في الأسفل

/*include <"iostream">

Main()
{
  std::out < "Line One/n";
  std::out < "Line Two/n";
  std::out < "Line Three/n";
  return 0;
}
// Output

Line One
Line Two
Line Three*/

//الحل

#include <iostream>

int main()
{
  std::cout << "Line One \n";
  std::cout << "Line Two \n";
  std::cout << "Line Three \n";
  return 0;
}